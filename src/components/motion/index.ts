/**
 * Motion primitives barrel. Import from "@/components/motion" — keeps page
 * components clean and lets us swap implementations without touching consumers.
 */

export { PinnedSection } from "./pinned-section";
export { HorizontalScroll } from "./horizontal-scroll";
export { ParallaxLayer } from "./parallax-layer";
export { Reveal } from "./reveal";
